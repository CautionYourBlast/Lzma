os:
    - linux
    - osx

language: generic

osx_image: xcode8.3

branches:
  only:
    - master
    - dev

script:
    - swift build -Xlinker -liconv
    - swift build -Xlinker -liconv --configuration=release
    - swift test -Xlinker -liconv
